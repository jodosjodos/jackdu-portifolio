

function DateContainer({title}:{title:string}) {
  return (
    <div className="flex w-fit items-center bg-[hsla(0,0%,100%,.15)] px-[20px] py-[10px]">
{title}
    </div>
  )
}

export default DateContainer